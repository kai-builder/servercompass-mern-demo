# ── Public variables (displayed in the React UI) ──────────────────────────────
APP_NAME=ServerCompass MERN Demo
API_URL=https://api.servercompass.app
ENVIRONMENT=production
VERSION=1.0.0

# ── Private variables (kept server-side, never sent to the browser) ───────────
DATABASE_URL=mongodb://user:password@localhost:27017/servercompass
API_SECRET_KEY=your-secret-key-here

# ── Server config ─────────────────────────────────────────────────────────────
PORT=3000
